import React from "react";

const styles = {
  wrapper: "text-2xl font-medium w-full bg-gray-200 m-3 rounded-md p-5",
};

export default function ActionInfoBtn() {
  return (
    <div className={styles.wrapper}>
      <p>Where to?</p>
    </div>
  );
}
